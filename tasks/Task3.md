# Task 3: generateStaticParams

Implement a dynamic version of `generateStaticParams` for your BookDetailsPage in order to prefetch the dynamic data at build time.

```ts
return [{id: '1'}, {id: '2'}]
```